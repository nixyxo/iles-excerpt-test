{
  "name": "iles-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": ">= 16.0.0"
  },
  "devDependencies": {
    "@islands/excerpt": "^0.1.3",
    "iles": "^0.7.19",
    "typescript": "^4.5",
    "vue-tsc": "^0.29"
  }
}
